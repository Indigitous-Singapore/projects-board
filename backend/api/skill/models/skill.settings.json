{
  "kind": "collectionType",
  "collectionName": "skills",
  "info": {
    "name": "Skill"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": false
  },
  "attributes": {
    "Title": {
      "type": "string",
      "required": true,
      "maxLength": 50
    },
    "positions": {
      "via": "skills",
      "collection": "position"
    }
  }
}
