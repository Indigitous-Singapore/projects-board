{
  "routes": [
    {
      "method": "GET",
      "path": "/causes",
      "handler": "cause.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/causes/count",
      "handler": "cause.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/causes/:id",
      "handler": "cause.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/causes",
      "handler": "cause.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/causes/:id",
      "handler": "cause.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/causes/:id",
      "handler": "cause.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
